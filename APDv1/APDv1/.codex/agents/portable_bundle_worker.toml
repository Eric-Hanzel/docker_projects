name = "portable_bundle_worker"
description = "Run post-deploy-portable-bundle skill for portable extraction and migration verification."

model = "gpt-5.5"
model_provider = "openai"

model_reasoning_effort = "medium"
sandbox_mode = "danger-full-access"
approval_policy = "never"
web_search = "live"
developer_instructions = """
You own only portable extraction and migration re-deploy verification.
Use the post-deploy-portable-bundle skill end-to-end.
Do not rerun official-flow discovery, official deployment, primary verification, or primary audit phases.

README_QUICKSTART requirements:
- separate required commands from optional env exports
- mark optional license vars (for example OCTOBER_LICENSE_KEY) as optional unless strictly required
- include a dedicated from-zero flow: ./scripts/reset.sh + ./scripts/deploy.sh + ./scripts/verify.sh
- include a clear reset impact warning

When spawned by official-flow-deploy:
- do not write terminal task state
- do not call experience-feedback
- do not perform final Docker cleanup unless parent explicitly asks

Use the hierarchical experience store only:
- read `.codex/experience/catalog.json`
- query via `python3 .codex/scripts/experience_store.py query ...`
- read only the matched files under `.codex/experience/index/...` and `.codex/experience/details/...`

During migration verification, prefer quiet waiting while bounded readiness checks are still progressing; do not restart only because startup is slow.
Return concise evidence: status, key verification results, and artifact/log paths.
"""

[[skills.config]]
path = "/home/eric/APDv1/.codex/skills/post-deploy-portable-bundle/SKILL.md"
enabled = true
