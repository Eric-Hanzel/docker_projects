# Local Deployment Task

The runner will provide one JSON object containing `url` and any necessary extras such as `license_key`.

Use the provided target and extras to deploy the project locally on this machine.

Required mode:

```text
delivery_mode=local-run
portable_final_required=false
```

Please follow the repository's normal local-run deployment workflow and finish with a verified local deployment that I can access from this machine.

Do not build a final portable bundle for this task.
